syntax = "proto3";

service Greeter {
  // Unary RPC: A single request gets a single response
  rpc SayHello (HelloRequest) returns (HelloReply);
}

message HelloRequest {
  string name = 1; // The name of the person to greet
}

message HelloReply {
  string message = 1; // The greeting message
}
